import { DistinctCategoriesPipe } from './distinct-categories.pipe';

describe('DistinctCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new DistinctCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
